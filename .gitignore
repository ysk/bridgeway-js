node_modules/

# デモのビルド成果物 (再生成: node build.demo.mjs)
sample/**/app.js
sample/todoapp/todo.js

# 一時ファイル
*.tgz
scratch-*.mjs

# CLI 動作確認で生成されるスキャフォールド(brg init の既定名)
/bridgey-app/

# dist/ は <script>/CDN 配布物なので追跡する（無視しない）
